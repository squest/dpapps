## Customize the test machine
machine:
  java:
    version: oraclejdk8

## Customize dependencies
dependencies:
  override:
    - lein deps
    - lein sub -s "arit" deps

## Customize test commands
test:
  override:
    - lein sub -s "arit" test
